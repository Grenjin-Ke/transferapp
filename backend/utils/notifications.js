const Notification = {
    notifyEmployees: (transaction) => {
      // Send notifications to all employees
    },
    notifyExecutives: (transactionId) => {
      // Send notification to the executive about the transaction status update
    }
  };
  
  module.exports = Notification;
  